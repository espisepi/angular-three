// Entry for the builder. Import the library entrypoint so the module registers elements.
import '@angular/elements';
import '../../../libs/web-components/src/index';

// Nota: la importación anterior activa el registro definido en el módulo de la librería.
