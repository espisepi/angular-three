import './vanilla-three-element.js';

// If Angular Elements are built and available, they can also be imported here.
try {
  // require the compiled Angular elements bundle if present
  // import('../../../dist/libs/web-components')
} catch (e) {
  // ignore if not present
}
